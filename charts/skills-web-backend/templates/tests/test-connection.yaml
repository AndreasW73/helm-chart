apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skills-web-backend.fullname" . }}-test-connection"
  labels:
    {{- include "skills-web-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skills-web-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
