apiVersion: v1
kind: Service
metadata:
  name: {{ include "skills-web-frontend.fullname" . }}
  labels:
    {{- include "skills-web-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skills-web-frontend.selectorLabels" . | nindent 4 }}
